# Simple micro-frontend expample based on: Webpack, TypeScript, Svelte, Module Federation, Sass

## Getting Started
```
$ npm install
$ npm start
```
The app will be available at `http://localhost:9000/`.